name: JavaScript CI

on:
  push:
    branches:
      - main

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Verify Files
        run: |
          echo "Repository cloned successfully"
          ls -la